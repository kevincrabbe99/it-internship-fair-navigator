# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting DEV URL on merge into dev branch
# "on":
#   push:
#     branches:
#       - dev
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - name: Build Docker image
#         run: |
#           cd fe
#           docker build -t itfn-fe:prod .
#       - name: Push Docker image to Firebase Hosting DEV URL
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: "${{ secrets.GITHUB_TOKEN }}"
#           firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_IT_INTERNSHIP_NAVIGATOR }}"
#           channelId: live
#           projectId: it-internship-navigator
#           entryPoint: ./fe
#           run: firebase deploy --only hosting:it-internship-navigator
